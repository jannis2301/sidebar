import { describe, it, expect } from 'vitest';

describe('Sidebar', () => {
  it('should be a sidebar element', () => {
    const sidebarElement = 'SidebarElement';
    expect(sidebarElement).toBe('SidebarElement');
  });
});
